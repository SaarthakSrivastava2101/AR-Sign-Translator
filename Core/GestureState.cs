namespace ARSignTranslator.Core
{
    public enum GestureState
    {
        Idle,
        Tracking,
        Arming,
        Capturing,
        Processing,
        Result,
        Cooldown,
        Reset
    }
}
